rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Morvello Cars Rental Management Platform
    // Allows authenticated team operations and web dashboard synchronization
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
